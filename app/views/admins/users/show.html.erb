<h2 class="mb-4">ユーザー詳細</h2>
<table class="table table-bordered table-striped table-hover w-50">
  <tr>
    <th style="width: 30%;">ID</th>
    <td><%= @user.id %></td>
  </tr>
  <tr>
    <th>名前</th>
    <td><%= @user.name %></td>
  </tr>
  <tr>
    <th>メール</th>
    <td><%= @user.email %></td>
  </tr>
  <tr>
    <th>登録日</th>
    <td><%= @user.created_at.strftime("%Y/%m/%d") %></td>
  </tr>
</table>
<h3 class="mt-5">このユーザーの投稿一覧</h3>
<% if @user.recipes.any? %>
  <ul class="list-group w-75">
    <% @user.recipes.each do |recipe| %>
      <li class="list-group-item d-flex justify-content-between align-items-center">
        <%= link_to recipe.title, admins_recipe_path(recipe) %>
        <small><%= recipe.created_at.strftime('%Y/%m/%d') %></small>
      </li>
    <% end %>
  </ul>
<% else %>
  <p class="text-muted">このユーザーのレシピはまだありません。</p>
<% end %>
<h3 class="mt-5">このユーザーのコメント一覧</h3>
<% if @user.comments.any? %>
  <ul class="list-group w-75">
    <% @user.comments.each do |comment| %>
      <li class="list-group-item d-flex justify-content-between align-items-center">
        <div>
          <strong>コメント:</strong><%= comment.body %><br>
          <strong>対象レシピ:</strong>
          <%= link_to comment.recipe.title, admins_recipe_path(comment.recipe) %>
        </div>
        <div class="d-flex align-items-center">
          <small class="me-3"><%= comment.created_at.strftime('%Y/%m/%d') %></small>
          <%= link_to "削除", admins_comment_path(comment), method: :delete, data: { confirm: "本当に削除しますか？" }, class: "btn btn-sm btn-danger" %>
        </div>
      </li>
    <% end %>
  </ul>
<% else %>
  <p class="text-muted">このユーザーのコメントはまだありません。</p>
<% end %>
<%= link_to "ユーザー一覧へ戻る", admins_users_path, class: "btn btn-outline-secondary mt-3" %>
<%= link_to "このユーザーを削除", admins_user_path(@user), method: :delete, data: { confirm: "本当に削除しますか？" }, class: "btn btn-danger mt-3" %>