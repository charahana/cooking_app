<div class="container mt-4">
  <h1 class="fw-bold text-info mb-4"><i class="fas fa-edit me-2"></i>レシピ編集</h1>
  <%= render "form", recipe: @recipe %>
  <div class="mt-4">
    <%= link_to "詳細に戻る", recipe_path(@recipe), class: "btn btn-outline-secondary" %>
  </dv>
</div>